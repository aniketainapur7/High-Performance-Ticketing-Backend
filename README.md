# High-Performance-Ticketing-Backend

/api-gateway        (Node + Express)
/booking-service    (Go + gRPC)
/infra              (docker, redis, postgres)
/docs               (design & flows)
docker-compose.yml